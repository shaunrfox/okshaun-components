import { type Config } from 'prettier';

const config: Config = {
  tabWidth: 2,
  useTabs: false,
  semi: true,
  singleQuote: true,
  trailingComma: 'all',
  printWidth: 80,
};

export default config;
